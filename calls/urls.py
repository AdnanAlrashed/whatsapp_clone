from django.urls import path
from . import views

app_name = 'calls'

urlpatterns = [
    # path('start/', views.start_call, name='start'),
    # path('join/<str:room_name>/', views.join_call, name='join'),
    # أضف مسارات أخرى حسب الحاجة
]
    # أضف مسارات أخرى حسب الحاجة